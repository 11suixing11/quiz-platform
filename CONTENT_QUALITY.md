# Content quality standard

这是新增和迁移测试的最低门槛。它把“有题目”与“值得被产品入口推荐”分开，避免继续用数量掩盖内容质量。

## 生命周期

| 状态 | 含义 | 产品入口 |
| --- | --- | --- |
| `flagship` | 经过双语、计分、结果解释和可达性审阅的核心测试 | 首页四条入口、相关推荐 |
| `standard` | 已符合统一模型，但仍在观察内容表现 | 候选公开路线（后续批次） |
| `review` | 可以运行，但存在待修订内容或计分问题 | 默认不在首页推荐 |
| `archive` | 暂不对外开放，保留源码供后续重写 | 不进入目录 |

当前产品保留 16 个旗舰入口；其余测试不会因为“已经存在”就自动成为推荐内容。

## 必检项

### 1. 双语完整性

- 标题、描述、每道题目和每个选项都同时拥有简体中文与 English。
- 中英文选项数量一致，题目和选项 ID 唯一。
- English 字段不得混入中文字符、日文字符或未翻译占位词。
- 两种语言表达同一测量意图，不用机器直译痕迹或仅改变几个词的伪翻译。

### 2. 计分可达性

- 每道题至少有两个选项，分值数量与选项数量一致。
- 最低、典型、最高答案组合都能返回对象结果，不能出现 `NaN`、`Infinity` 或空结果。
- `type` 测试必须返回可定位的类型 key；`dimensions` 测试必须返回可读的维度分数；`score` 测试必须返回有限的数值分数。
- 反向题要在题目内容或代码中明确表达，不能依赖隐含约定。

### 3. 结果解释

- 结果至少包含一段双语解释，且与实际计分形态一致。
- 维度结果使用稳定的中英文标签，不直接把内部缩写当作用户文案。
- 分数结果提供连续、无重叠的分数区间，并覆盖实际有效范围。
- 结果文案描述倾向和当下状态，不把测试包装成诊断、治疗或固定身份。

### 4. 体验与边界

- 题目数量、预计时长和 registry 元数据一致。
- 键盘、移动端和 reduced-motion 状态下仍可完成主流程。
- 敏感主题使用低压力、非诊断表达，并在结果页保留自我反思边界。
- 新测试先进入 `review`，完成审阅后才能升级为 `standard` 或 `flagship`。

## 审阅输出

运行：

```bash
npm run audit:flagship
```

脚本会检查 16 个旗舰测试，输出每项的 `ready`、`review` 或 `blocked` 状态，以及下一步迁移清单。警告不会阻断本地开发；结构性错误会以非零退出码结束。
